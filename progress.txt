# Ralph Progress Log - AI News Aggregator

## Project Overview
AI News Aggregator & Newsletter Platform - Curated AI news from top newsletters and blogs, deduplicated and delivered as personalized email digests.

## Tech Stack
- Frontend: Next.js 14 (App Router) + Tailwind CSS
- Backend: Next.js API Routes
- Database: Supabase (PostgreSQL)
- Email Delivery: Resend + React Email
- AI Processing: Claude API (content extraction)
- Email Ingestion: Mailgun Inbound Routing
- Hosting: Vercel

## Codebase Patterns
- Project uses src/ directory structure with App Router
- TypeScript strict mode enabled
- Tailwind CSS for styling
- Inter font as default
- Supabase clients: use `createClient()` from `@/lib/supabase/client` for browser, `@/lib/supabase/server` for server-side
- Use `createServiceRoleClient()` for admin operations that bypass RLS
- Migrations: `supabase/migrations/YYYYMMDDHHMMSS_description.sql` format
- Use `IF NOT EXISTS` for tables/indexes to make migrations idempotent
- Reuse `update_updated_at_column()` trigger function for updated_at columns
- UI components go in `src/components/ui/` directory with `'use client'` directive

## Learnings
(Ralph will append learnings here after each iteration)

---

## 2026-02-11 - US-001
- **Implemented:** Initialize Next.js 14 project with Tailwind CSS
- **Files created:**
  - package.json (Next.js 14.2.21, React 18, TypeScript, Tailwind)
  - tsconfig.json (strict mode, bundler module resolution, @/* path alias)
  - tailwind.config.ts (configured for src/ directory)
  - postcss.config.mjs (autoprefixer + tailwindcss)
  - next.config.mjs (basic Next.js config)
  - .eslintrc.json (next/core-web-vitals)
  - .gitignore (standard Next.js ignores)
  - src/app/layout.tsx (Inter font, basic metadata)
  - src/app/page.tsx (placeholder landing page)
  - src/app/globals.css (Tailwind directives)
- **Learnings for future iterations:**
  - npm project names cannot contain capital letters - use lowercase with hyphens
  - Dev server confirmed working on port 3000
  - Typecheck passes with `npm run typecheck`
---

## 2026-02-11 - US-002
- **Implemented:** Set up Supabase client configuration
- **Files changed:**
  - package.json (added @supabase/supabase-js and @supabase/ssr)
  - src/lib/supabase/client.ts (browser client using createBrowserClient)
  - src/lib/supabase/server.ts (server client with cookie handling + service role client)
  - .env.example (Supabase environment variables)
- **Learnings for future iterations:**
  - Use @supabase/ssr package for Next.js App Router compatibility
  - Browser client: createBrowserClient from @supabase/ssr
  - Server client needs cookie handling for session management
  - Service role client bypasses RLS - use only in secure server contexts
---

## 2026-02-11 - US-003
- **Implemented:** Create sources table schema
- **Files created:**
  - supabase/migrations/20260211000001_create_sources_table.sql
- **Schema details:**
  - Created `source_type` enum with values: 'newsletter', 'blog'
  - Table columns: id (UUID PK), name (text unique), slug (text unique), type (source_type enum), description, audience, reach, feed_url, intake_email, icon_url, is_active (default true), created_at, updated_at
  - Indexes on slug and is_active
  - Trigger for auto-updating updated_at column
- **Learnings for future iterations:**
  - Supabase migrations go in `supabase/migrations/` directory
  - Use timestamp prefix for migration files (YYYYMMDDHHMMSS format)
  - Use `IF NOT EXISTS` for safe re-runnable migrations
  - `update_updated_at_column()` trigger function can be reused across tables
---

## 2026-02-11 - US-004
- **Implemented:** Create articles table schema
- **Files created:**
  - supabase/migrations/20260211000002_create_articles_table.sql
- **Schema details:**
  - Table columns: id (UUID PK), source_id (FK to sources), headline, summary, url, url_hash, tags (text array), image_url, published_at, ingested_at, raw_email_id, extraction_confidence (float default 1.0)
  - Indexes: idx_articles_url_hash (unique), idx_articles_source_published, idx_articles_published, idx_articles_tags (GIN)
- **Learnings for future iterations:**
  - Use GIN index for array columns (like tags) to enable efficient array search
  - DESC ordering in indexes (e.g., published_at DESC) optimizes common "latest first" queries
  - url_hash unique index enables efficient deduplication of articles
---

## 2026-02-11 - US-005
- **Implemented:** Create users table schema
- **Files created:**
  - supabase/migrations/20260211000003_create_users_table.sql
- **Schema details:**
  - Created `email_frequency` enum with values: 'daily', 'weekly'
  - Table columns: id (UUID PK), email (text unique), email_verified (default false), verification_token, unsubscribe_token (unique), frequency (email_frequency enum default 'daily'), is_active (default true), signed_up_at, last_email_sent_at, created_at
  - Indexes: idx_users_email, idx_users_active_frequency (partial index WHERE is_active=true), idx_users_unsubscribe_token (partial index WHERE NOT NULL)
- **Learnings for future iterations:**
  - Partial indexes (with WHERE clause) can optimize specific query patterns
  - `idx_users_active_frequency` uses partial index to only index active users, optimizing digest batch queries
  - `idx_users_unsubscribe_token` uses partial index to skip NULL values, as most lookups only need non-null tokens
---

## 2026-02-11 - US-006
- **Implemented:** Create user_subscriptions table schema
- **Files created:**
  - supabase/migrations/20260211000004_create_user_subscriptions_table.sql
- **Schema details:**
  - Table columns: id (UUID PK), user_id (FK to users with ON DELETE CASCADE), source_id (FK to sources), created_at
  - Unique constraint on (user_id, source_id) to prevent duplicate subscriptions
  - Indexes on user_id and source_id for efficient lookups
- **Learnings for future iterations:**
  - ON DELETE CASCADE ensures subscriptions are cleaned up when a user is deleted
  - Named constraint (`user_subscriptions_user_source_unique`) makes debugging easier than auto-generated names
---

## 2026-02-11 - US-007
- **Implemented:** Create user_article_log table schema
- **Files created:**
  - supabase/migrations/20260211000005_create_user_article_log_table.sql
- **Schema details:**
  - Table columns: id (UUID PK), user_id (FK to users with ON DELETE CASCADE), article_id (FK to articles), sent_at, digest_batch_id
  - Unique constraint on (user_id, article_id) to prevent sending same article twice
  - Indexes: idx_ual_user_article (for dedup checks), idx_ual_user_sent (for querying sent history)
- **Learnings for future iterations:**
  - The user_article_log table is critical for deduplication - prevents sending the same article to a user multiple times
  - digest_batch_id can be used to group articles sent in the same digest email
---

## 2026-02-11 - US-008
- **Implemented:** Create system_config table schema
- **Files created:**
  - supabase/migrations/20260211000006_create_system_config_table.sql
- **Schema details:**
  - Table columns: key (TEXT PK), value (JSONB), updated_at (TIMESTAMPTZ)
  - Uses simple key-value structure for flexible configuration storage
  - Reuses update_updated_at_column() trigger function for auto-updating updated_at
- **Learnings for future iterations:**
  - system_config table uses TEXT primary key directly (no separate UUID id column) since keys are natural primary keys
  - JSONB type allows storing complex configuration values with query capability
---

## 2026-02-11 - US-009
- **Implemented:** Create waitlist table schema
- **Files created:**
  - supabase/migrations/20260211000007_create_waitlist_table.sql
- **Schema details:**
  - Table columns: id (UUID PK), email (TEXT UNIQUE), created_at (TIMESTAMPTZ)
  - Indexes: idx_waitlist_email (for email lookups), idx_waitlist_created_at (for position ordering)
- **Learnings for future iterations:**
  - Waitlist table is simpler than users table - just email and timestamp, no tokens needed
  - created_at index enables efficient calculation of waitlist position
---

## 2026-02-11 - US-010
- **Implemented:** Create ingestion_log table schema
- **Files created:**
  - supabase/migrations/20260211000008_create_ingestion_log_table.sql
- **Schema details:**
  - Created `ingestion_type` enum with values: 'email_received', 'rss_polled', 'extraction_success', 'extraction_failed', 'duplicate_skipped'
  - Table columns: id (UUID PK), source_id (FK to sources, nullable for system-level events), type (ingestion_type enum), details (JSONB), created_at
  - Indexes: idx_ingestion_log_source_id, idx_ingestion_log_type, idx_ingestion_log_created_at (DESC), idx_ingestion_log_source_type (composite)
- **Learnings for future iterations:**
  - Use `DO $$ ... EXCEPTION WHEN duplicate_object ... END $$` pattern to safely create enums idempotently
  - source_id is nullable in ingestion_log since some log entries may not be source-specific
  - Composite index (source_id, type) optimizes common filter patterns like "all extraction failures for source X"
---

## 2026-02-11 - US-011
- **Implemented:** Seed sources table with 9 initial sources
- **Files created:**
  - supabase/migrations/20260211000009_seed_sources.sql
- **Seed details:**
  - 6 newsletters: The Rundown AI, Superhuman AI, TLDR AI, Ben's Bites, The Neuron, Import AI
  - 3 blogs: OpenAI Blog, Anthropic Blog, Google DeepMind Blog
  - Each source has name, slug, type, description, audience, reach populated
  - Blog sources include feed_url pointing to their RSS feeds
- **Learnings for future iterations:**
  - Use `ON CONFLICT (column) DO UPDATE SET` pattern to make seed migrations idempotent
  - Escape single quotes in SQL strings by doubling them (e.g., "Ben''s Bites")
  - RSS feed URLs for major AI labs: openai.com/blog/rss.xml, anthropic.com/rss.xml, deepmind.google/blog/rss.xml
---

## 2026-02-11 - US-012
- **Implemented:** Seed system_config with initial settings
- **Files created:**
  - supabase/migrations/20260211000010_seed_system_config.sql
- **Seed details:**
  - `max_subscribers`: `{"limit": 3000, "enabled": true}`
  - `subscriptions_paused`: `{"paused": false, "message": "We've hit capacity! Join the waitlist."}`
  - Uses `ON CONFLICT DO UPDATE` pattern for idempotent seeding
- **Learnings for future iterations:**
  - JSONB values can be cast inline with `::jsonb` (e.g., `'{"key": "value"}'::jsonb`)
  - system_config uses `key` as primary key, so conflict resolution is on (key)
---

## 2026-02-11 - US-013
- **Implemented:** Create TypeScript types for database schema
- **Files created:**
  - src/types/database.ts
- **Type details:**
  - Enum types: SourceType, Frequency, IngestionType
  - Table interfaces: Source, Article, User, UserSubscription, UserArticleLog, SystemConfig, Waitlist, IngestionLog
  - Insert types: *Insert variants for creating new records (with optional auto-generated fields)
  - Update types: *Update variants for partial updates
- **Learnings for future iterations:**
  - TypeScript types go in `src/types/` directory
  - Use string type for UUID fields (database returns them as strings via JSON)
  - Use string type for TIMESTAMPTZ fields (database returns ISO date strings via JSON)
  - Provide Insert and Update type variants for flexibility when creating/updating records
---

## 2026-02-11 - US-014
- **Implemented:** Create design system color constants
- **Files changed:**
  - tailwind.config.ts (added custom colors in theme.extend.colors)
- **Color palette:**
  - cream: #FAFAF7 (background)
  - lavender: #B8A9E8 (primary accent)
  - lavender-hover: #9B87D6 (primary hover state)
  - mint: #A8D8C8 (secondary accent)
  - peach: #F5C5A3 (tertiary accent)
  - charcoal: #2D2D2D (primary text)
  - warm-gray: #6B6B6B (secondary text)
  - light-gray: #9B9B9B (muted text)
  - border-gray: #E8E8E4 (borders)
  - error-rose: #E8A0A0 (error states)
  - success-green: #A0D8B8 (success states)
- **Learnings for future iterations:**
  - Custom colors in Tailwind go in `theme.extend.colors` object
  - Use hyphenated names for multi-word colors (e.g., `lavender-hover` becomes `bg-lavender-hover` in Tailwind classes)
  - Color values can be used with any Tailwind utility: bg-*, text-*, border-*, etc.
---

## 2026-02-11 - US-015
- **Implemented:** Create reusable Button component
- **Files created:**
  - src/components/ui/Button.tsx
- **Component features:**
  - Primary variant: lavender background, white text, lavender-hover on hover
  - Secondary variant: white background, charcoal text, border-gray border
  - Loading state with animated SVG spinner
  - Disabled state with reduced opacity and cursor-not-allowed
  - 8px border-radius (rounded-lg), 12px 24px padding (py-3 px-6)
  - Focus ring styling for accessibility
  - forwardRef for ref forwarding support
- **Learnings for future iterations:**
  - UI components go in `src/components/ui/` directory
  - Use `forwardRef` for components that need to forward refs to underlying DOM elements
  - Set `displayName` on forwardRef components for better debugging in React DevTools
  - `'use client'` directive needed for components using client-side features (event handlers, refs)
---

## 2026-02-11 - US-016
- **Implemented:** Create reusable Input component
- **Files created:**
  - src/components/ui/Input.tsx
- **Component features:**
  - White background with border-gray border
  - Charcoal text color with light-gray placeholder
  - Lavender focus ring and border on focus
  - Error state with error-rose border and focus ring
  - forwardRef for ref forwarding support
  - Consistent styling with Button component (rounded-lg, px-4 py-3)
- **Learnings for future iterations:**
  - Input components follow same pattern as Button: forwardRef, displayName, 'use client'
  - Use `error` prop (boolean) to toggle error state styling
  - Transition-colors for smooth state changes on focus/hover
---

## 2026-02-11 - US-017
- **Implemented:** Create SourceCard component
- **Files created:**
  - src/components/SourceCard.tsx
- **Component features:**
  - Displays source name, description, reach, audience
  - Custom checkbox that toggles selected state
  - Selected state: lavender left border (3px), faint lavender background (8% opacity via bg-lavender/[0.08])
  - Hover state: elevated shadow (hover:shadow-md)
  - Keyboard accessible with Enter/Space support
  - Uses role="checkbox" and aria-checked for accessibility
- **Learnings for future iterations:**
  - Non-UI components (page-specific components) go in `src/components/` (not `src/components/ui/`)
  - Use Tailwind arbitrary values for precise opacity: `bg-lavender/[0.08]` for 8% opacity
  - Use mixed borders with `border-l-[3px]` for specific side border width
  - line-clamp-2 utility truncates text to 2 lines with ellipsis
---

## 2026-02-12 - US-018
- **Implemented:** Create FrequencySelector component
- **Files created:**
  - src/components/FrequencySelector.tsx
- **Component features:**
  - Two pill-shaped buttons (Daily, Weekly) using `rounded-full px-6 py-2`
  - Selected state: lavender fill with white text (`bg-lavender text-white`)
  - Unselected state: white fill, charcoal text, border (`bg-white text-charcoal border border-border-gray`)
  - Helper text below showing schedule description based on selection
  - Keyboard accessible with focus ring styling
  - Uses role="radiogroup" and role="radio" with aria-checked for accessibility
- **Learnings for future iterations:**
  - Use `rounded-full` for pill-shaped buttons in Tailwind
  - Radio group pattern: use `role="radiogroup"` on container and `role="radio"` with `aria-checked` on individual options
  - FrequencySelector follows similar pattern to SourceCard (page-specific component in `src/components/`)
---

## 2026-02-12 - US-019
- **Implemented:** Create page header component with logo
- **Files created:**
  - src/components/Header.tsx
- **Component features:**
  - Brand name displayed as text logo ("AI Digest") with bold charcoal text
  - Tagline: "Your AI news, curated & deduplicated." in warm-gray
  - Subtitle: "Pick your sources. Choose your frequency. That's it." in light-gray
  - Responsive text sizing (text-3xl on mobile, text-4xl on md+)
  - Centered layout with text-center
- **Learnings for future iterations:**
  - Header component follows same pattern as other page-specific components in `src/components/`
  - Use `&amp;` and `&apos;` for HTML entities in JSX to avoid lint warnings
  - Responsive typography: use md: prefix for larger screens (e.g., `text-3xl md:text-4xl`)
---

## 2026-02-12 - US-020
- **Implemented:** Create page footer component
- **Files created:**
  - src/components/Footer.tsx
- **Component features:**
  - Displays "Unsubscribe anytime" text
  - Centered layout with text-center
  - Muted gray text using text-light-gray
  - Smaller text size (text-sm) for footer styling
- **Learnings for future iterations:**
  - Footer follows same simple pattern as Header component
  - Use text-sm for footer text to give it appropriate visual hierarchy
---

## 2026-02-12 - US-021
- **Implemented:** Build GET /api/sources endpoint
- **Files created:**
  - src/app/api/sources/route.ts
- **Endpoint features:**
  - GET /api/sources returns all active sources
  - Sources grouped by type into `newsletters` and `blogs` arrays
  - Returns fields: id, name, slug, type, description, audience, reach
  - Uses server-side Supabase client from `@/lib/supabase/server`
  - Includes error handling for database errors
- **Learnings for future iterations:**
  - API routes go in `src/app/api/[endpoint]/route.ts` using Next.js 14 App Router pattern
  - Use `NextResponse.json()` for JSON responses with optional status options
  - Import `createClient` from `@/lib/supabase/server` for server-side API routes
  - TypeScript's `Pick<T, K>` utility type useful for defining response shapes with subset of fields
---

## 2026-02-12 - US-022
- **Implemented:** Build GET /api/subscription-status endpoint
- **Files created:**
  - src/app/api/subscription-status/route.ts
- **Endpoint features:**
  - GET /api/subscription-status returns subscription availability status
  - Reads max_subscribers and subscriptions_paused from system_config table
  - Counts active, verified users to check against cap
  - Counts waitlist entries for display
  - Returns `{ accepting: boolean, waitlistCount: number }`
  - Uses typed interfaces for system config values (MaxSubscribersConfig, SubscriptionsPausedConfig)
- **Learnings for future iterations:**
  - Use `{ count: 'exact', head: true }` with Supabase select to get count without fetching rows
  - `.in('key', [...])` filters on multiple values efficiently in a single query
  - Define TypeScript interfaces for JSONB config values to ensure type safety
---

## 2026-02-12 - US-023
- **Implemented:** Build POST /api/subscribe endpoint - validation
- **Files created:**
  - src/app/api/subscribe/route.ts
- **Endpoint features:**
  - POST /api/subscribe validates email, sources array, and frequency
  - Email validation: checks for valid email format using regex
  - Sources validation: checks array has at least 1 valid slug from database
  - Frequency validation: checks value is 'daily' or 'weekly'
  - Returns 400 with specific error messages for each invalid field
  - Returns all validation errors in a single response (not just the first)
- **Learnings for future iterations:**
  - Use `ValidationError` interface with `field` and `message` for clear error handling
  - Collect all validation errors before returning, better UX than one-at-a-time
  - Sources validation requires DB check - verify slugs exist and are active
  - Return validated/normalized data in success response for downstream use
---

## 2026-02-12 - US-024
- **Implemented:** Build POST /api/subscribe endpoint - cap check
- **Files changed:**
  - src/app/api/subscribe/route.ts (added cap check and waitlist logic)
- **Endpoint features:**
  - Checks system_config for max_subscribers and subscriptions_paused settings
  - Counts active, verified users to compare against cap limit
  - If at capacity (paused or cap reached): inserts email into waitlist table
  - Handles duplicate waitlist entries gracefully (returns existing position)
  - Returns `{ waitlisted: true, position: N }` when added to waitlist
  - Position calculated from waitlist count
- **Learnings for future iterations:**
  - Supabase returns error code 'PGRST116' when `.single()` query finds no rows
  - Use `.lte('created_at', timestamp)` to calculate position relative to when user joined waitlist
  - Normalize email to lowercase before storing to prevent duplicate entries
  - Reuse config parsing pattern from subscription-status endpoint
---

## 2026-02-12 - US-025
- **Implemented:** Build POST /api/subscribe endpoint - user creation
- **Files changed:**
  - src/app/api/subscribe/route.ts (added user creation and subscription logic)
- **Endpoint features:**
  - Checks if email already exists in users table with friendly messages for different states
  - Generates UUID for verification_token and unsubscribe_token using `crypto.randomUUID()`
  - Inserts user into users table with email_verified=false
  - Gets source IDs from validated slugs
  - Inserts user_subscriptions for each selected source using batch insert
  - Handles unique constraint violation (race condition) with friendly duplicate message
  - Returns `{ success: true, message: 'Check your email to confirm your subscription!' }`
- **Learnings for future iterations:**
  - Use `crypto.randomUUID()` for generating UUIDs in Node.js (built-in, no package needed)
  - Handle different user states: verified+active, pending verification, inactive
  - PostgreSQL error code '23505' indicates unique constraint violation
  - Use Map to efficiently look up source IDs from slugs for batch insert
  - Create subscription records in batch with array of objects
---

## 2026-02-12 - US-026
- **Implemented:** Set up Resend email client
- **Files created:**
  - src/lib/email/resend.ts
- **Files changed:**
  - package.json (added resend package)
  - .env.example (added RESEND_API_KEY and FROM_EMAIL)
- **Features:**
  - Installed resend package for email sending
  - Created Resend client initialized with RESEND_API_KEY environment variable
  - Exported `sendEmail` wrapper function with clean interface:
    - Takes to, subject, html/react content, optional from, replyTo, and headers
    - Returns `{ success: boolean, id?: string, error?: string }`
  - Handles errors gracefully and logs to console
  - Exported raw `resend` client for advanced use cases
- **Learnings for future iterations:**
  - Email lib goes in `src/lib/email/` directory
  - Resend uses `replyTo` not `reply_to` in their API (camelCase)
  - Support both `html` and `react` content for email rendering (react for React Email templates)
  - FROM_EMAIL env var provides default sender address if not specified per-email
---

## 2026-02-12 - US-027
- **Implemented:** Create verification email template
- **Files created:**
  - src/emails/VerificationEmail.tsx
- **Files changed:**
  - package.json (added @react-email/components)
- **Template features:**
  - Subject line: "Confirm your AI Digest subscription" (via Preview component)
  - Lavender (#B8A9E8) CTA button "Confirm Subscription"
  - Clean, centered layout with cream background (#FAFAF7)
  - Heading, welcome text, CTA button, and footer with expiration notice
  - Uses verificationUrl prop to include the verification link
  - Inline styles for email client compatibility
- **Learnings for future iterations:**
  - Email templates go in `src/emails/` directory
  - Use @react-email/components for building email templates
  - Use inline styles (not Tailwind) for email compatibility across clients
  - Preview component sets the email preview text shown in inbox
  - Use `as const` for textAlign in style objects for TypeScript
  - Export both named and default export for flexibility
---

## 2026-02-12 - US-028
- **Implemented:** Send verification email on signup
- **Files changed:**
  - src/app/api/subscribe/route.ts (added sendEmail call with VerificationEmail template)
  - .env.example (added NEXT_PUBLIC_APP_URL documentation)
- **Features:**
  - After user creation, sends verification email via Resend
  - Email includes link to /api/verify?token={verification_token}
  - Verification URL uses NEXT_PUBLIC_APP_URL env var (defaults to localhost:3000 for dev)
  - Email send failures are logged but don't fail the request (user was already created)
  - Uses React Email template for the verification email content
- **Learnings for future iterations:**
  - Import React Email templates directly and pass to sendEmail's `react` option
  - Use NEXT_PUBLIC_APP_URL env var for building absolute URLs in emails
  - Handle email send failures gracefully - log error but return success since the primary action (user creation) succeeded
  - Consider queueing retries or admin notifications for email failures in production
---

## 2026-02-12 - US-029
- **Implemented:** Build GET /api/verify endpoint for email confirmation
- **Files created:**
  - src/app/api/verify/route.ts
- **Endpoint features:**
  - GET /api/verify?token={verification_token} verifies user's email
  - Looks up user by verification_token
  - Sets email_verified=true and clears verification_token
  - Redirects to /verify-success on successful verification
  - Redirects to / with error=invalid_token if token not found
  - Redirects to / with error=verification_failed if update fails
  - Handles already-verified users gracefully (redirects to success page)
- **Learnings for future iterations:**
  - Use `NextResponse.redirect()` for redirect responses in Next.js API routes
  - Build redirect URLs using `new URL(path, request.url)` for proper URL construction
  - Use `searchParams.set()` to add query parameters to URLs
  - PGRST116 error code from Supabase means "not found" for `.single()` queries
---

## 2026-02-12 - US-030
- **Implemented:** Build GET /api/unsubscribe endpoint for one-click unsubscribe
- **Files created:**
  - src/app/api/unsubscribe/route.ts
- **Endpoint features:**
  - GET /api/unsubscribe?token={unsubscribe_token} unsubscribes a user
  - Looks up user by unsubscribe_token
  - Sets is_active=false to deactivate the user
  - Redirects to /unsubscribed on successful unsubscription
  - Redirects to / with error=invalid_token if token not found
  - Redirects to / with error=unsubscribe_failed if update fails
  - Handles already-unsubscribed users gracefully (redirects to unsubscribed page)
- **Learnings for future iterations:**
  - Unsubscribe endpoint follows same pattern as verify endpoint - token lookup, update, redirect
  - One-click unsubscribe is important for email compliance (CAN-SPAM, GDPR)
  - Both endpoints use is_active (unsubscribe) vs email_verified (verify) for different purposes
---

## 2026-02-12 - US-031
- **Implemented:** Build GET /api/preferences endpoint
- **Files created:**
  - src/app/api/preferences/route.ts
- **Endpoint features:**
  - GET /api/preferences?token={unsubscribe_token} fetches user preferences
  - Validates token query param is provided (returns 400 if missing)
  - Looks up user by unsubscribe_token
  - Returns 404 if token is invalid
  - Fetches user's subscribed source IDs from user_subscriptions table
  - Fetches full source details for active sources
  - Returns `{ frequency: Frequency, sources: SourcePreference[] }`
- **Learnings for future iterations:**
  - Avoid Supabase foreign key joins for TypeScript type safety - better to do separate queries
  - Use `.in('id', array)` to fetch multiple records by ID efficiently
  - Preferences endpoint uses unsubscribe_token for authentication (same token used in email links)
---

## 2026-02-12 - US-032
- **Implemented:** Build PUT /api/preferences endpoint
- **Files changed:**
  - src/app/api/preferences/route.ts (added PUT handler)
- **Endpoint features:**
  - PUT /api/preferences updates user's subscription preferences
  - Requires token in request body (unsubscribe_token)
  - Validates frequency if provided (must be 'daily' or 'weekly')
  - Validates sources if provided (must be non-empty array of valid slugs)
  - Updates user's frequency in users table
  - Replaces user_subscriptions (deletes existing, inserts new) for new source selections
  - Returns `{ success: true, message: 'Preferences updated successfully' }`
  - Uses service role client for write operations to bypass RLS
- **Learnings for future iterations:**
  - Use service role client (`createServiceRoleClient()`) for admin write operations
  - Delete-then-insert pattern is simple for replacing subscriptions (no need for upsert logic)
  - Validate all source slugs exist before deleting existing subscriptions to prevent data loss
  - PUT endpoint accepts token in body unlike GET which uses query params
---

## 2026-02-12 - US-033
- **Implemented:** Build landing page - source selection section
- **Files changed:**
  - src/app/page.tsx (completely rewritten as client component with source selection)
- **Features:**
  - Fetches sources from /api/sources on mount using useEffect
  - Displays newsletters and blogs in separate sections with section headers
  - Uses SourceCard component for each source with checkbox selection
  - Selected sources tracked in state using Set<string>
  - "Select All" / "Deselect All" toggle button below source lists
  - Loading state with "Loading sources..." message
  - Error state with error message display
  - Uses Header and Footer components
  - Cream background with max-w-3xl centered container
- **Learnings for future iterations:**
  - Use 'use client' directive for pages with client-side state and effects
  - Convert API response data to component-compatible shapes with helper functions
  - Use Set<string> for efficient membership checking of selected IDs
  - Conditional rendering for section visibility (sources.newsletters.length > 0)
---

## 2026-02-12 - US-034
- **Implemented:** Build landing page - frequency and email section
- **Files changed:**
  - src/app/page.tsx (added FrequencySelector, email input, and subscribe button)
- **Features:**
  - FrequencySelector component integrated with Daily/Weekly options
  - Daily frequency selected by default via `useState<Frequency>('daily')`
  - Email input field with placeholder 'your@email.com'
  - Subscribe button positioned next to email input using flex layout
  - Helper text: "We'll send a confirmation email. No spam, ever."
  - New state: `frequency` and `email` for controlled inputs
- **Learnings for future iterations:**
  - Use `&apos;` for apostrophes in JSX text to avoid lint warnings
  - Flex layout with `flex gap-3` and `flex-1` provides responsive email + button arrangement
  - Import Frequency type from database.ts to ensure type safety with FrequencySelector
---

## 2026-02-12 - US-035
- **Implemented:** Build landing page - form submission
- **Files changed:**
  - src/app/page.tsx (added form submission, validation, and error handling)
- **Features:**
  - Wrapped content in `<form onSubmit={handleSubmit}>` element
  - Added client-side validation for sources (at least 1) and email format
  - Submit to POST /api/subscribe endpoint with email, sources (slugs), and frequency
  - Button shows loading spinner via `isLoading={isSubmitting}` prop
  - Validation errors displayed below relevant fields:
    - Source error: "Please select at least one source." (centered below Select All)
    - Email error: "Please enter a valid email address." (below input)
  - Handle server validation errors from response
  - Handle success, waitlist, and existing user response states
  - Added state: `isSubmitting`, `fieldErrors`, `submitMessage`, `submitSuccess`
- **Learnings for future iterations:**
  - Use `getSelectedSlugs()` to convert selected IDs to slugs for API submission
  - Button component uses `isLoading` prop (not `loading`) for loading state
  - Client-side validation runs first, then API validation on server
  - Use `Record<string, string>` for field-keyed error messages
  - Subscribe API returns `{ errors: ValidationError[] }` for 400 responses
---

## 2026-02-12 - US-036
- **Implemented:** Build landing page - success state
- **Files changed:**
  - src/app/page.tsx (added success state UI with conditional rendering)
- **Features:**
  - Added `submittedEmail` state to track the email used during successful signup
  - Success card displays when `submitSuccess` is true
  - Mint green checkmark icon: circular bg-mint background with white SVG checkmark
  - "Check your inbox!" heading in charcoal bold text
  - Message showing user's email address with instruction to click confirmation link
  - White card with rounded corners and shadow for visual prominence
- **Learnings for future iterations:**
  - Track form input values separately (like submittedEmail) to display in success states
  - Use early return pattern for conditional full-page renders (e.g., `if (submitSuccess) return <SuccessView />`)
  - SVG checkmark: use `strokeWidth={3}` for bold checkmarks, path `d="M5 13l4 4L19 7"`
  - Use `&apos;` for apostrophes in JSX strings to avoid lint warnings
---

## 2026-02-12 - US-037
- **Implemented:** Build landing page - waitlist state
- **Files changed:**
  - src/app/page.tsx (added waitlist mode UI with conditional rendering)
- **Features:**
  - Added subscription status check on page load via /api/subscription-status
  - Parallel fetch of subscription status and sources for efficiency
  - New state: `subscriptionStatus`, `waitlistPosition`, `waitlistJoined`, `isWaitlistMode`
  - Waitlist mode shows when `accepting=false`: hides source selection and frequency
  - Waitlist message card with peach alert icon and "We've hit capacity!" heading
  - Shows waitlist count ("X people ahead of you")
  - Email-only form with "Join Waitlist" button
  - Waitlist joined confirmation card with lavender clock icon
  - Shows waitlist position after successful join (Position #N)
  - Form validation adapts to mode (sources not required in waitlist mode)
- **Learnings for future iterations:**
  - Use `Promise.all()` to fetch multiple APIs in parallel for better performance
  - Create computed value `isWaitlistMode` to simplify conditional logic throughout component
  - Reuse existing form submission logic by adapting validation based on mode
  - Different icon colors for different states: mint=success, lavender=waiting, peach=alert
  - SVG clock icon path: `d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"`
---

## 2026-02-12 - US-038
- **Implemented:** Build verify-success page
- **Files created:**
  - src/app/verify-success/page.tsx
- **Features:**
  - Shows mint green checkmark icon (same style as landing page success state)
  - "You're all set!" heading in charcoal bold text
  - Message explaining digest will arrive based on selected frequency (daily/weekly)
  - Link to manage preferences page with note about using digest email link
  - Reuses Header and Footer components for consistent layout
  - White card with rounded corners and shadow for visual prominence
- **Learnings for future iterations:**
  - Server components (no 'use client') work well for static pages like verify-success
  - Reuse same visual patterns from landing page for consistency (card style, icons, colors)
  - Preferences link needs token param which users get from digest emails (can't link directly without token)
---


## 2026-04-01 - US-039
- **Implemented:** Build preferences page (display)
- **Files changed:**
  - src/app/preferences/page.tsx (already existed with full implementation)
- **Features verified:**
  - Reads token from URL query params via useSearchParams()
  - Fetches /api/sources and /api/preferences?token=... in parallel
  - Shows all source cards with user's subscribed sources pre-checked
  - Shows FrequencySelector with user's current frequency pre-selected
  - Loading and error states handled
  - Wrapped in Suspense for useSearchParams() compatibility
- **Learnings for future iterations:**
  - useSearchParams() requires Suspense boundary in Next.js 14 App Router
  - Fetch all sources from /api/sources (not just subscribed ones) so user can change selections
  - Initialize selectedSourceIds from preferences API response, not from sources API
---

## 2026-04-01 - US-065
- What was implemented: Fixed `normalizeUrl` in `src/lib/utils/url.ts` to only lowercase the hostname, not the full URL. Also made tracking param removal case-insensitive (handles `UTM_SOURCE`, `UTM_MEDIUM`, etc.)
- Files changed: `src/lib/utils/url.ts`
- **Learnings for future iterations:**
  - `normalizeUrl` previously called `url.toLowerCase()` before parsing, corrupting case-sensitive path tokens (e.g., newsletter redirect tokens like `/AbCdEf`)
  - Fix: `new URL(url)` then `parsed.hostname = parsed.hostname.toLowerCase()`
  - Tracking param stripping now uses `key.toLowerCase()` for case-insensitive comparison
---

## 2026-04-01 - US-066
- **Implemented:** Fix digest email link styling - make headlines visually clickable
- **Files changed:**
  - src/emails/DigestEmail.tsx (changed `headline` style: color '#2D2D2D' → '#B8A9E8', textDecoration 'none' → 'underline')
- **Learnings for future iterations:**
  - Email link visibility: react-email `<Link>` elements need explicit color and textDecoration since email clients strip default browser link styles
  - Footer links already had lavender + underline — headline links were missing the same treatment
---

## 2026-04-01 - US-067
- **Implemented:** Fix TLDR truncation and cache takeaways per batch
- **Files changed:**
  - src/emails/DigestEmail.tsx (replace nested <span> bullet with plain text `• ${takeaway}`; remove tldrBullet style)
  - src/lib/digest/sendDigest.ts (accept keyTakeaways as parameter instead of calling generateTLDR per-user)
  - src/lib/jobs/sendDigests.ts (generate TLDR once before user loop; check/populate digest_cache table)
  - supabase/migrations/20260401000001_create_digest_cache_table.sql (new: digest_cache table with cache_key unique index)
- **Learnings for future iterations:**
  - React Email <Text> elements do not support nested <span> children in all email clients — use template literals for inline text instead
  - digest_cache table uses cache_key format `{YYYY-MM-DD}-{frequency}` (e.g. '2026-04-01-daily') for batch deduplication
  - When moving per-user logic to batch level, pass the result as a parameter to lower-level functions rather than re-computing in each call
  - Supabase `.single()` returns null data (not error) when no row found — safe to check `if (cachedEntry)` without error handling
---

## 2026-04-01 - US-068
- **Implemented:** Fix raw_emails migration and verify subscriber cap counter
- **Files changed:**
  - supabase/migrations/20260401000002_create_raw_emails_table.sql (new: raw_emails table with all required columns + indexes)
  - src/app/api/subscribe/route.ts (fix new waitlist entry position: insert with .select('created_at').single() then count WHERE created_at <= new entry's created_at)
- **Verification:**
  - subscription-status/route.ts already correctly counts `is_active=true AND email_verified=true` users — no change needed
  - Waitlist position for existing entries already used position-relative count (lte query) — only new entry path was broken
- **Learnings for future iterations:**
  - When inserting a row and immediately needing it for a follow-up query, chain `.select('created_at').single()` on the insert to get the row data back
  - Waitlist position = COUNT(*) WHERE created_at <= new_entry.created_at (not total count)
---

## 2026-04-01 - US-069
- **Implemented:** Weekly digest: themes-only AI summary with no individual article links
- **Files created:**
  - src/lib/digest/generateWeeklySummary.ts (Claude Haiku call returning { themes: [{title, summary, sources}] })
  - src/emails/WeeklyDigestEmail.tsx (separate template: logo + date range, story count, AI disclaimer, theme sections with source pills, footer only)
  - supabase/migrations/20260401000003_add_themes_to_digest_cache.sql (add JSONB themes column to digest_cache)
- **Files changed:**
  - src/lib/digest/sendDigest.ts (added frequency, weeklyThemes, weeklyDateRange params; routes to WeeklyDigestEmail for weekly users)
  - src/lib/jobs/sendDigests.ts (weekly branch: calls generateWeeklySummary, caches with key `weekly-YYYY-WW`, passes themes to sendDigest; ISO week number helper)
  - src/components/FrequencySelector.tsx (weekly description updated to "Every Monday — a curated AI summary of the week's key themes. No individual article links.")
  - src/app/api/verify/route.ts (selects frequency from user, passes as ?frequency= query param on redirect to /verify-success)
  - src/app/verify-success/page.tsx (async page reading searchParams; daily shows "Your first digest arrives tomorrow morning", weekly shows "Your first weekly summary arrives next Monday")
- **Learnings for future iterations:**
  - digest_cache table now has both takeaways (TEXT[]) for daily and themes (JSONB) for weekly — check which column applies when reading cache
  - Weekly cache key format is `weekly-YYYY-WW` (ISO week number, zero-padded); daily is `YYYY-MM-DD-daily`
  - ISO week number calculation: shift day to Thursday, then diff from Jan 4 (always in week 1)
  - verify-success page uses Next.js 15 async searchParams (Promise<{...}>) — must await before reading
  - For email templates that don't use article links, still pass groupedArticles for article logging purposes; just don't render them in the template
---

## 2026-04-03 - US-063
- **Implemented:** Add mobile responsive styles to landing page
- **Files changed:**
  - src/app/page.tsx (changed `py-8 sm:py-12` → `py-4 sm:py-12` in all container divs for 16px vertical padding on mobile)
  - src/components/SourceCard.tsx (changed `p-5` → `p-4 sm:p-5` for reduced card padding on mobile)
- **Changes made:**
  - Source cards were already full-width on mobile via single-column grid (no explicit grid-cols-* = defaults to 1 column)
  - Frequency pills were already stacking vertically on mobile via `flex flex-col sm:flex-row`
  - Email input and button were already stacking vertically via `flex flex-col sm:flex-row` with `w-full sm:w-auto`
  - Added `py-4` (16px) on mobile for vertical padding across all page state containers
  - Reduced SourceCard internal padding to `p-4` (16px) on mobile
- **Learnings for future iterations:**
  - Tailwind `grid` without `grid-cols-*` defaults to 1 column, so source cards were already full-width on all screen sizes
  - Most mobile responsiveness was already implemented via `sm:` breakpoint prefixes in earlier stories
  - `py-4` = 16px vertical padding, `px-4` = 16px horizontal padding — both target 16px mobile padding
---

## 2026-04-03 - US-070
- **Implemented:** Admin dashboard at /ops with subscriber stats, recent digests, pipeline health, Claude usage, and action buttons
- **Files created:**
  - supabase/migrations/20260403000004_create_claude_usage_log.sql (new table: date, model, input_tokens, output_tokens)
  - src/app/ops/page.tsx (server component, ADMIN_SECRET auth via ?secret= query param)
  - src/app/api/ops/route.ts (POST handler for admin actions: toggle-signups, trigger-ingest, trigger-digest-daily/weekly, deactivate-user, promote-waitlist)
- **Files modified:**
  - .env.example (added ADMIN_SECRET variable)
- **Learnings for future iterations:**
  - Server components can't set HTTP status codes directly; return JSX with "401 Unauthorized" content for auth failures
  - Root layout always wraps pages - don't return <html>/<body> from page components
  - Inline React style objects use camelCase properties; style strings are not valid as React style props
  - Supabase join syntax: `.select('id, related_table(field)')` works with the service role client
  - For admin trigger actions that need CRON_SECRET, call internal API endpoints via fetch with the secret in x-cron-secret header using `getBaseUrl(request)` to derive the host
  - `crypto.randomUUID()` is available globally in Node.js 18+ (no import needed)
---

## 2026-04-03 - US-071
- **Implemented:** Remove hardcoded article limit and make digest article count configurable
- **Files changed:**
  - src/lib/digest/getUnsentArticles.ts (default limit changed from 20 to 50)
  - src/lib/jobs/sendDigests.ts (daily limit 20→50, weekly limit 40→50)
- **Learnings for future iterations:**
  - The `limit` parameter in `getUnsentArticles` controls how many articles max a user can receive per digest; the function fetches `limit * 2` from DB to account for already-sent articles before filtering
  - Daily and weekly digest callers each pass explicit limits; changing the default in `getUnsentArticles` only affects direct callers that omit the argument
---

## 2026-04-03 - US-072
- **Implemented:** Strip HTML boilerplate before Claude extraction to reduce token usage
- **Files changed:**
  - src/lib/ai/anthropic.ts (added `stripHtmlBoilerplate` helper + call before truncation)
- **What was done:**
  - Added `stripHtmlBoilerplate(html: string): string` exported helper using regex to remove `<style>`, `<head>`, `<script>`, and `<noscript>` tag blocks
  - Called `stripHtmlBoilerplate` on `htmlContent` before the 50000 char truncation step
  - The 50000 char limit applies to the stripped content
- **Learnings for future iterations:**
  - `stripHtmlBoilerplate` is exported and can be unit tested or reused elsewhere
  - The regex uses `[\s\S]*?` (lazy dot-all) to handle multiline tag content correctly
---

## 2026-04-03 - US-073
- **Implemented:** Add plain text fallback to digest emails
- **Files changed:**
  - src/lib/email/resend.ts (added `text` field to SendEmailOptions, passed through to resend.emails.send)
  - src/lib/digest/sendDigest.ts (added `buildDailyPlainText` and `buildWeeklyPlainText` helpers, pass text to sendEmail)
- **What was done:**
  - Added `text?: string` to `SendEmailOptions` interface and forwarded it to the Resend API call
  - `buildDailyPlainText`: header with date/count, Key Takeaways section (if present), articles grouped by source with headline/summary/URL/tags
  - `buildWeeklyPlainText`: header with dateRange/count, themes with title/summary/sources
  - Both builders return joined newlines for clean plain text
- **Learnings for future iterations:**
  - `WeeklyTheme` has `title`, `summary`, `sources` — NOT `theme` or `articles`
  - Resend accepts both `html` and `text` fields simultaneously for multipart MIME
---

## 2026-04-03 - US-074
- What was implemented: Verified CRON_SECRET missing returns 401 — implementation was already in place from prior iterations
- Files changed: prd.json (marked passes: true), progress.txt
- **Learnings for future iterations:**
  - src/lib/auth/cron.ts already correctly returns false (not throws) when CRON_SECRET is undefined/empty
  - All three cron endpoints (process-emails, rss, digest/send) already return 401 { error: 'Unauthorized' } when validateCronSecret returns false
  - No code changes were needed; story was already implemented as a byproduct of prior cron endpoint work
---

## 2026-04-03 - US-075
- What was implemented: Updated Claude URL extraction prompt to prefer newsletter redirect URLs instead of guessing original article URLs
- Files changed:
  - src/lib/ai/anthropic.ts (updated url field instruction in buildUserPrompt)
- **What was done:**
  - Changed the url field description from "Direct URL to the original source (not the newsletter link, but the underlying article/announcement)" to "The direct link from the newsletter HTML (use the newsletter redirect URL as-is — do not try to guess or reconstruct the original article URL). If multiple links exist for an item, prefer the most specific one."
  - Removed the parenthetical that instructed Claude to find the "underlying article" which caused it to guess/infer URLs that were often wrong
- **Learnings for future iterations:**
  - Newsletter redirect URLs are more reliable than Claude guessing original article URLs from context
  - Prompt wording strongly influences Claude's URL selection behavior — be explicit about using redirect URLs as-is
---
